import { RegisterForm } from "@/src/features/auth/components/RegisterForm";

export const metadata = {
  title: "Create Account | Mini CRM",
  description: "Create a new Mini CRM account",
};

export default function RegisterPage() {
  return <RegisterForm />;
}
